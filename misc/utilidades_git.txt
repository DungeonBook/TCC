// caso a permissão não de certo
git config user.name "Eduarda Rocha Prestes"
git config user.email "rpeduarda09@gmail.com"


//caso o commit não de certo
git add .
git commit -m "feat: atualizacoes e correcoes de erro"
git push origin main


//Comando que deve ser rodado caso dê problema de permissão na pasta arquivos
ls -la
chmod -R o+w arquivos 
